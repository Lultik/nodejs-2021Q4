const { CustomReadableStream } = require('./customReadableStream');
const { CustomWritableStream } = require('./customWritableStream');

module.exports = {CustomReadableStream, CustomWritableStream};
